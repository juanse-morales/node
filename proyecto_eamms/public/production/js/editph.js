/* $(document).on('ready',function(){
  $('#edit').on('click',function(){
    $('.even pointer selected').attr('id');
    var id = $('')
    alert(id);
  });
}) */

function edit(){
  alert('editaarr');
}